
import { useEffect, useState } from "react";

export async function mainFunction(currentTime) {

    window.requestAnimationFrame(main)
  return ( console.log(currentTime));
}


export default mainFunction;